> **Provenance.** Written in the `claude-skills` repo before the 2026-08-25 split; the skills it covers now live in `sondalab-ai/skillkit`. Mentions of `claude-skills` below are historical and describe the catalog at the time of writing.

# mind-gym Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `mind-gym` skill that proposes profile-calibrated puzzles, guides their resolution Socratically, offers non-preachy growth insights, and routes technical puzzles to a tool while keeping conceptual ones in the terminal.

**Architecture:** Pure content skill (no runtime code). One `SKILL.md` carrying the workflow plus two `references/*.md` files (voice rules, routing heuristic). Persistent state lives in `~/.claude/mind-gym/` as JSON, read/written directly by the agent at runtime. Puzzles are model-generated with a hidden pre-committed solution.

**Tech Stack:** Markdown + YAML frontmatter (parsed by `gray-matter` in the installer). README catalog regenerated via `npm run docs`. Validation via `npm test` (node `--test`).

---

## File Structure

- `skills/mind-gym/SKILL.md` — frontmatter (name + trigger-rich description) and the full workflow.
- `skills/mind-gym/references/tone.md` — voice rules for guidance and growth insights.
- `skills/mind-gym/references/domains.md` — technical-vs-conceptual routing heuristic and dynamic-domain inference.
- `README.md` — regenerated catalog table (auto, do not hand-edit between markers).

Constraints from the spec, enforced in every file below:
- Output language is inferred by the agent at runtime; no hardcoded default language.
- Files are self-contained: no mention of any other skill by name.

---

### Task 1: Create the skill body (`SKILL.md`)

**Files:**
- Create: `skills/mind-gym/SKILL.md`

- [ ] **Step 1: Write the file**

Create `skills/mind-gym/SKILL.md` with exactly this content:

````markdown
---
name: mind-gym
description: >
  Cognitive training against brain atrophy. Proposes a puzzle calibrated to the
  user's profile, guides the resolution empathetically with Socratic hints (never
  spoiling the answer), offers a transferable growth insight, and routes technical
  puzzles to the right tool (Python for math, REPL/editor + tests for programming)
  while keeping logic and humanistic puzzles in the terminal. USE when the user asks
  to train/exercise their brain or mind, wants a puzzle, riddle, brain teaser, kata,
  or cognitive workout, says they feel mentally rusty, or asks to "keep me sharp" /
  "give me a challenge" / "propose a problem to solve". Maintains a persistent
  profile and history under ~/.claude/mind-gym/ to adapt difficulty and rotate
  domains across sessions.
---

# mind-gym — Cognitive training

Propose a puzzle suited to the user, guide its resolution without spoiling it, then
offer a short transferable insight. It is a **persistent trainer**: profile and
history survive across sessions and drive adaptive difficulty and domain rotation.

> **Output language.** Present everything in the user's language, inferred from the
> conversation. Do not hardcode a default language.

## Persistent state

Stored under `~/.claude/mind-gym/` (create the directory on first use):

- `profile.json`
  - `profession` / `interests` — free text, used to infer dynamic domains.
  - `levels` — map `domain -> integer level` (1 = easy). Adaptive difficulty.
  - `prefs` — `{ "sessionLength": 1, "tone": "...", "timeBudget": "..." }`.
- `history.jsonl` — one JSON object per line per puzzle:
  `{ "date", "domain", "difficulty", "outcome", "attempts", "hash" }`
  where `outcome` is `solved` | `partial` | `gave-up` and `hash` is a short stable
  hash of the puzzle statement, used to avoid re-proposing the same puzzle.

Read these files directly. If missing, treat the profile as empty.

## Domain taxonomy (open)

Base domains: `logic` (deduction, lateral thinking), `math` (numeric/algebraic/
combinatorial), `programming` (bug-hunt, algorithms, katas), `humanities` (ethics,
philosophy, writing, etymology, verbal reasoning).

When the profile matches none of these (e.g. a physician), infer a relevant
**dynamic domain** at runtime (e.g. clinical reasoning) and route it by the
heuristic in `references/domains.md` — do not force-fit a base domain.

## Tool routing

Decide by one heuristic, not a fixed list (details in `references/domains.md`):

- **Conceptual / verbal** (logic, humanities, non-technical dynamic domains)
  → stay in the **terminal**.
- **Computational** (math, programming, technical dynamic domains) → **open the tool**:
  - math → Python via Bash (also used for objective verification).
  - programming → editor / REPL + runnable tests.

## Workflow

Run in order.

### 1. Load profile

Read `~/.claude/mind-gym/`. If the profile is missing or the context is unknown, ask
for: profession, domain of interest, level, time available. Offer to save it.

### 2. Pick domain + difficulty

- Difficulty: start from `levels[domain]` (default 2). Adapt from history.
- Rotate domains for breadth across sessions; honor an explicit user request.

### 3. Generate the puzzle with a hidden pre-commit

Before showing anything, fix internally the **solution + verification method**. Keep
them hidden. The puzzle must be well-formed: a single unambiguous answer that is
checkable. Skip any statement whose `hash` is already in `history.jsonl`.

### 4. Route the tool

Apply the routing heuristic. For math, prepare to verify with Python; for
programming, set up runnable tests.

### 5. Present the puzzle

State it clearly in the user's language. Do not reveal hints or the solution yet.

### 6. Guide (Socratic ladder)

Escalate only as needed: stimulus question → nudge → clue → subproblem → full
solution. Reveal the **full solution only on explicit request or after 3 stuck
attempts**. Follow the voice rules in `references/tone.md`.

### 7. Verify the answer

- Technical → run a script / tests (objective). For math, compute in Python.
- Conceptual → reasoned comparison against the pre-committed solution.

### 8. Growth insight (optional, not preachy)

Offer 1–2 lines linking the technique used to a transferable thinking pattern
("want a takeaway?"). Obey `references/tone.md`: no inflated praise, no lecturing,
no "you should have…".

### 9. Update state

Append a record to `history.jsonl`. Recalibrate `levels[domain]`: bump up on a fast
solve, down when the user got stuck. Persist `profile.json`.

### 10. Continue or close

Default session = 1 puzzle. Offer the next one or stop.

## Defaults

- Full solution after **3** stuck attempts (or on request).
- Session = **1** puzzle, then offer the next.
````

- [ ] **Step 2: Verify the frontmatter parses**

Run:
```bash
node -e "import('gray-matter').then(({default:m})=>{const d=m(require('fs').readFileSync('skills/mind-gym/SKILL.md','utf8'));console.log(d.data.name); if(d.data.name!=='mind-gym')process.exit(1)})"
```
Expected: prints `mind-gym`, exit code 0.

- [ ] **Step 3: Commit**

```bash
git add skills/mind-gym/SKILL.md
git commit -m "feat(mind-gym): add cognitive-training skill body"
```

---

### Task 2: Create the voice rules (`references/tone.md`)

**Files:**
- Create: `skills/mind-gym/references/tone.md`

- [ ] **Step 1: Write the file**

Create `skills/mind-gym/references/tone.md` with exactly this content:

```markdown
# Voice rules — guidance and growth insights

Empathetic and constructive, never condescending. The user is a capable adult
training their mind, not a student being graded.

## Guidance (Socratic ladder)

- Ask before telling. A good question beats a given answer.
- Escalate one rung at a time: stimulus question → nudge → clue → subproblem →
  full solution. Do not skip ahead.
- Never reveal the solution preemptively. Full solution only on explicit request
  or after 3 stuck attempts.
- Validate partial progress honestly, without inflating it.

## Growth insight

- Optional. Offer it, don't impose it ("want a takeaway?").
- 1–2 lines. Link the technique just used to a transferable thinking pattern.
- About the method, not the person.

## Banned moves

- Inflated praise ("Amazing!", "Genius!").
- Lecturing or moralizing.
- Hindsight scolding ("you should have seen that").
- Spoiling the answer to look helpful.
- Hedging filler that adds no information.
```

- [ ] **Step 2: Commit**

```bash
git add skills/mind-gym/references/tone.md
git commit -m "docs(mind-gym): add voice rules reference"
```

---

### Task 3: Create the routing reference (`references/domains.md`)

**Files:**
- Create: `skills/mind-gym/references/domains.md`

- [ ] **Step 1: Write the file**

Create `skills/mind-gym/references/domains.md` with exactly this content:

```markdown
# Domains and tool routing

## The heuristic

Route by the nature of the puzzle, not by a fixed domain list:

- **Conceptual / verbal** → solve in the terminal (text only).
  Examples: logic deductions, lateral-thinking riddles, ethics dilemmas, writing,
  etymology, verbal reasoning, and any dynamic domain whose work is argument or
  language rather than computation.
- **Computational** → open a tool.
  - Math → Python via Bash. Also use it to verify the answer objectively.
  - Programming → an editor / REPL plus runnable tests that prove correctness.
  - Any dynamic technical domain whose answer is checkable by running something.

When unsure, ask: "can the answer be verified by running code or a calculation?"
Yes → computational (open a tool). No → conceptual (terminal).

## Base domains

- `logic` — deduction, lateral thinking. Conceptual.
- `math` — numeric, algebraic, combinatorial. Computational.
- `programming` — bug-hunt, algorithms, katas. Computational.
- `humanities` — ethics, philosophy, writing, etymology, verbal reasoning. Conceptual.

## Dynamic domains

When the user's profession or interests match none of the base domains, infer a
relevant domain at runtime instead of forcing a base one. Examples:

- Physician → clinical reasoning (differential diagnosis). Conceptual.
- Lawyer → case analysis, statutory interpretation. Conceptual.
- Data engineer → query optimization, schema design. Computational.

Classify the inferred domain with the heuristic above, then route accordingly.
Store the inferred domain name in `profile.json` under `levels` so difficulty
adapts like any other domain.
```

- [ ] **Step 2: Commit**

```bash
git add skills/mind-gym/references/domains.md
git commit -m "docs(mind-gym): add domains and routing reference"
```

---

### Task 4: Regenerate the README catalog and validate

**Files:**
- Modify: `README.md` (auto-generated between `<!-- SKILLS:START -->` / `<!-- SKILLS:END -->`)

- [ ] **Step 1: Regenerate the catalog**

Run:
```bash
npm run docs
```
Expected: command exits 0.

- [ ] **Step 2: Verify mind-gym appears in the catalog**

Run:
```bash
grep -c '`mind-gym`' README.md
```
Expected: prints `1` (or higher), exit code 0.

- [ ] **Step 3: Run the installer test suite**

Run:
```bash
npm test
```
Expected: all tests pass (exit code 0). If a test enumerates skills, confirm
`mind-gym` is picked up and nothing regressed.

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: add mind-gym to skills catalog"
```

---

## Self-Review

**1. Spec coverage:**
- Persistent trainer (profile + history) → Task 1 state section + workflow steps 1, 9. ✓
- Model-generated puzzles with hidden pre-commit → Task 1 step 3. ✓
- Open taxonomy + dynamic domains → Task 1 taxonomy, Task 3. ✓
- Tool routing (requirements c/d) → Task 1 routing, Task 3 heuristic. ✓
- Socratic guidance, 3-attempt threshold → Task 1 step 6, Task 2. ✓
- Objective verification → Task 1 step 7. ✓
- Non-preachy growth insight → Task 1 step 8, Task 2. ✓
- Output language inferred, self-contained files → enforced in all files, no other
  skill named. ✓
- YAGNI (no gamification) → not introduced anywhere. ✓
- README catalog regeneration → Task 4. ✓

**2. Placeholder scan:** No TBD/TODO; all file contents are complete and inlined.

**3. Type consistency:** State shape (`profile.json` keys `profession`/`interests`/
`levels`/`prefs`; `history.jsonl` keys `date`/`domain`/`difficulty`/`outcome`/
`attempts`/`hash`) is identical between Task 1's state section and workflow steps.
`levels[domain]` referenced consistently in steps 2 and 9 and in Task 3.

No gaps found.
```
